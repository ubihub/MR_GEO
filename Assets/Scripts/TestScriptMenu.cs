using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestScriptMenu : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        string thisThing = this.gameObject.name;
        string newString = thisThing;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
